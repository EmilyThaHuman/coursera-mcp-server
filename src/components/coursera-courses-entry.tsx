import React from 'react';
import ReactDOM from 'react-dom/client';
import CourseraCourses from './coursera-courses';

const root = document.getElementById('root');
if (root) {
  ReactDOM.createRoot(root).render(
    <React.StrictMode>
      <CourseraCourses />
    </React.StrictMode>
  );
}









